# GIS application
Application created using typescript, react and tailwind.

## Starting the project
- run `npm install` in the root folder
- add .env file with variable `VITE_API_BASE_URL=`
- run the app with `npm run dev` or if you wanna see it in production way, run `npm run build` and then `npm run preview`
